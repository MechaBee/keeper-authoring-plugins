# `tree_panel` Component

```yaml
kind: tree_panel
title: Agent hierarchy
data_source: agent_hierarchy
empty_message: No hierarchy available.
```

Required: provider source resolving to `tree`. Optional: `title`, `description`, and
`empty_message`. Hierarchy shape is owned by the documented provider resource.
