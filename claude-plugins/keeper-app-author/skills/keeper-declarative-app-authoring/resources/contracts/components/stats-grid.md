# `stats_grid` Component

```yaml
kind: stats_grid
title: Overview
data_source: overview_stats
empty_message: No metrics available.
```

Required: provider source resolving to `stats`. Optional: `title`, `description`, and
`empty_message`. This component does not calculate metrics; the documented provider resource does.
