# `inbox_thread_panel` Component

```yaml
kind: inbox_thread_panel
title: Conversation
data_source: inbox_thread
empty_message: No thread selected.
```

Required: provider source resolving to `inbox_thread`. Optional: `title`, `description`, and
`empty_message`. Reply/intervention buttons normally come from a provider-generated action set.
