# `timeline_panel` Component

```yaml
kind: timeline_panel
title: History
data_source: work_timeline
empty_message: No events yet.
```

Required: provider source resolving to `timeline`. Optional: `title`, `description`, and
`empty_message`. Use only with a documented timeline resource.
